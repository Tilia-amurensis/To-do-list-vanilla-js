import { handleListClick, addTasks, deleteCheck } from "./tasks-list.js";
import { updateTodoList, updateTaskCount } from "./todo-list.js";

document.addEventListener("DOMContentLoaded", () => {
  console.log("App initialized");
});